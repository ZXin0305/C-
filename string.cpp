/*
* 字符串
*/

#include<iostream>
#include<string>
using namespace std;

int Index(string s, string t, int poi) {
	int n, m, i;
	string sub;
	if (poi > 0) {
		n = s.length();  //得到主串的长度
		m = s.length();  //得到子串的长度
		i = poi;
		while (i <= n - m + 1) {

		}
	}
}

int main() {
	string s = "zx0305";
	system("pause");
	return 0;
}